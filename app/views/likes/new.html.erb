<h1>Add Like</h1>

<% form_with(model: [@tweet, @like], local: true) do |form| %>
  <%= form.label :user_id, "UserID:" %></br>
  <%= form.text_field :user_id %></br></br>

  <%= form.submit %>

<% end %>

<%= link_to 'Back', tweet_likes_path(@tweet) %>
